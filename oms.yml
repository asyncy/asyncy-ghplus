omg: 1
actions:
  languages:
    help: Get a users top languages
    format: languages {handler}
    arguments:
      handler:
        type: string
        required: true

environment:
  api_token:
  user_agent:
    default: Undefined
